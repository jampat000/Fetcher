[pytest]
# Repo root must be on sys.path for `from app...` imports (CI + local).
pythonpath = .
testpaths = tests
markers =
    no_auth_override: use real require_auth (tests upgrade / no-password flows)
